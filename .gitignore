/.vs
/Build
/build
/build-emscripten
/build-emscripten-*
*.user
.vscode
*.internal
/.cppbuild
/*.sln

# Emscripten build output includes a --preload-file bundle of whatever local
# gamedata path was passed to CMake at configure time. UT99/UE1 game data is
# commercial and must never be committed - see Docs/VR/WEBXR_IMPLEMENTATION_PLAN.md.
*.data

# Regenerated by every web/smoke_test_webgpu.py run - not a source artifact.
web/webgpu_smoke_screenshot.png

# npm devDependency (iwer, for smoke_test_webxr.py's headless WebXR
# emulation) - installed via `npm install` in web/, not vendored.
web/node_modules
